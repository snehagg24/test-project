---
kind: pipeline
type: kubernetes
name: default

platform:
  os: linux
  arch: ppc64le

clone:
  disable: true

steps:
- name: clone
  commands:
  - git clone https://github.com/snehakpersistent/test-project.git .

- name: greeting
  commands:
  - echo hello

trigger:
  branch:
  - main
