---
kind: pipeline
type: ssh
name: default

platform:
  os: linux
  arch: ppc64le

server:
  host:
    from_secret: host
  user:
    from_secret: user
  ssh_key:
    from_secret: ssh_key
clone:
  disable: true

steps:
- name: clone
  commands:
  - git clone https://github.com/snehakpersistent/test-project.git .

- name: greeting
  commands:
  - echo hello

trigger:
  branch:
  - main
